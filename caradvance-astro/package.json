{
  "name": "caradvance-astro",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "mkdir -p public/autoink public/auto && cp -r ../autoink/. public/autoink/ && cp -r ../auto/. public/auto/ && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "shiki": "^1.29.2"
  }
}
